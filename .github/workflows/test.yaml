name: CI
on:  [workflow_dispatch, pull_request, push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps: [uses: answerdotai/workflows/nbdev-ci@master]
    
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Fix nbdev dependencies
#         run: pip install -Uq --force-reinstall 'nbdev<3' execnb
#       - uses: answerdotai/workflows/nbdev-ci@master


