# Formatting


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

------------------------------------------------------------------------

<a
href="https://github.com/mustafaslanCoto/peshbeen/blob/main/peshbeen/formatting.py#L235"
target="_blank" style="float:right; font-size:smaller">source</a>

### cov_table

``` python

def cov_table(
    cov_matrixes, font_size_px:int=350
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/mustafaslanCoto/peshbeen/blob/main/peshbeen/formatting.py#L213"
target="_blank" style="float:right; font-size:smaller">source</a>

### inject_header_table_groups

``` python

def inject_header_table_groups(
    gt_main, columns, subtitle_text, col_min_px:int=260, gap_px:int=16, font_px:int=12
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/mustafaslanCoto/peshbeen/blob/main/peshbeen/formatting.py#L193"
target="_blank" style="float:right; font-size:smaller">source</a>

### gt_mini

``` python

def gt_mini(
    df_small, title:NoneType=None, show_col_labels:bool=True
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/mustafaslanCoto/peshbeen/blob/main/peshbeen/formatting.py#L101"
target="_blank" style="float:right; font-size:smaller">source</a>

### make_var_gt_regimes

``` python

def make_var_gt_regimes(
    df, n_regimes:int, inner_labels:NoneType=None, param_names:NoneType=None, regime_labels:NoneType=None,
    decimals:NoneType=None, p_threshold:float=0.001, title:str='Model summary',
    subtitle:str='Multi-regime coefficients'
):

```

*Build a great_tables GT that groups columns into n_regimes with
identical visible* inner labels under each regime tab_spanner.

Parameters - arr_or_df: numpy array (n_rows x (n_regimes \* n_inner)) or
pandas DataFrame columns must be ordered regime1..regimeN. - n_regimes:
int, number of regimes - inner_labels: list of strings, inner column
headings (default \[‘Coef’,‘Std. Err’,‘t-stat’,‘p-value’\]) -
param_names: optional list of row index labels (len == n_rows) -
regime_labels: optional list of regime labels for the spanners (default
‘Regime 1’, ‘Regime 2’, …) - decimals: dict mapping inner label -\>
decimal places (e.g. {‘Coef’:4, ‘t-stat’:3}) - p_threshold: float,
p-value threshold to show as “\<threshold” - title/subtitle: strings for
the table header

Returns: GT object (ready to render in Jupyter)

------------------------------------------------------------------------

<a
href="https://github.com/mustafaslanCoto/peshbeen/blob/main/peshbeen/formatting.py#L9"
target="_blank" style="float:right; font-size:smaller">source</a>

### make_main_gt

``` python

def make_main_gt(
    df, n_regimes:int, inner_labels:NoneType=None, param_names:NoneType=None, regime_labels:NoneType=None,
    decimals:NoneType=None, p_threshold:float=0.001, title:str='Model summary',
    subtitle:str='Multi-regime coefficients'
):

```

*Build a great_tables GT that groups columns into n_regimes with
identical visible* inner labels under each regime tab_spanner.

Parameters - arr_or_df: numpy array (n_rows x (n_regimes \* n_inner)) or
pandas DataFrame columns must be ordered regime1..regimeN. - n_regimes:
int, number of regimes - inner_labels: list of strings, inner column
headings (default \[‘Coef’,‘Std. Err’,‘t-stat’,‘p-value’\]) -
param_names: optional list of row index labels (len == n_rows) -
regime_labels: optional list of regime labels for the spanners (default
‘Regime 1’, ‘Regime 2’, …) - decimals: dict mapping inner label -\>
decimal places (e.g. {‘Coef’:4, ‘t-stat’:3}) - p_threshold: float,
p-value threshold to show as “\<threshold” - title/subtitle: strings for
the table header

Returns: GT object (ready to render in Jupyter)
